- ran into shader issue with logarithmic depth buffering causing shaders to simply render fully transparent objects
- needing to use '../' instead of './' in order for Vite to build font paths correctly, or else they end up as '/assets/fonts' instead of '/fonts'
- adventures in sun glow, which led to anti-aliasing, which led to layering issues, and possible need for lens flares?